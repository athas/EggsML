#!/bin/sh

cd $CONCIEGGS_IRC_DIR

echo "/whois $1" > in
for i in $(seq 1 2); do
    authed=$(tail -n 10 out | perl -ne "if (/$1 (\\S+) is logged in as/) { print \$1; exit(0); }")
    if [ "$authed" ]; then
        echo $authed
        exit
    fi
    sleep 1
done
