#!/usr/bin/env tclsh

foreach {month day} [split [clock format [clock seconds] -format "%m %d"]] {
    set ha [expr {$month == 12 && $day <= 25 ? "Ho" : "HA"}]
    puts [string repeat $ha [expr {2 + int(rand() * 10)}]]
}
