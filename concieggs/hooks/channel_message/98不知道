#!/usr/bin/env rust-script
//! -*- Rust -*-
//!
//! 当有人说“知道”的时候，说“我不知道！”。
//!
//! ```cargo
//! [dependencies]
//! ```

use std::env::var;

fn main() {
    if var("EGGS_BODY").map(|s| s.to_string().contains("知道")).unwrap_or(false) {
        println!("我不知道！");
    }
}
