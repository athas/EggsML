#!/bin/sh

charmen=$(dbRead charmen)
num=$(awk "BEGIN{print 0.1 * $charmen}")
maybeGood $num.0 || exit

support=$(dbUserRead "$EGGS_USER" "support")
if [ "$support" = "1" ]; then
   support "$EGGS_USER"
fi
