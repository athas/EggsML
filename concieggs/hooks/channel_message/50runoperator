#!/bin/sh

set -f

echo 0, $@

regex="(?i)(^ *$CONCIEGGS_NAME[,:!] +(?<before>[^:]+):(?<operator>[^:]+): +(?<after>.+)$)\$"

eval "$(echo "$EGGS_BODY" | parse "$regex")"

if [ "after" ]; then
    runoperator "operator" "$before" "$after"
fi
