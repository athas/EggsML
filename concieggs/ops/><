#!/usr/bin/env python
#
# Crossfade kommandoer.

import sys
import eggsthon.shell

def interleave(xs, ys):
    return xs + ys

print interleave(*[eggsthon.shell.runcmd(c.split()) for c in sys.argv[1:3]])
