#!/usr/bin/env perl
use 5.020;
use strict;
use warnings;
use utf8::all;

use EggsML::EggsPI list => ['cowsay'];

my @lines = <STDIN>;
@lines = cowsay({ stdin => \@lines }, "-n");
chomp(@lines);

say $_ for @lines;
