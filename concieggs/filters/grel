#!/bin/sh
":"; exec emacs --quick --script "$0" "$@" # -*-emacs-lisp-*-
(add-to-list 'load-path (concat (getenv "CONCIEGGS_LIB_DIR") "/emeggslisp"))
(require 'concieggs)

(run-on-stdin 'upcase)
(kill-emacs 0)
