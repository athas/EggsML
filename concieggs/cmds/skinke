#!/bin/sh
#
# Bedre visdom

visdom="$(cat $CONCIEGGS_DB_DIR/fakta | grep -i ".*$1" | randomElement)"
if [ "$visdom" ]; then
    new="$visdom"
    while [ "$new" = "$visdom"]; do
        new=$(echo "$visdom" | skinkefy)
    done
    echo "$new"
else
    echo "Hm, tjah... næh."
fi
