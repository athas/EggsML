#!/bin/sh

lookup() {
     cat $CONCIEGGS_DB_DIR/fakta | grep -F -i $1
}

if [ "$1" ]; then
    if ! lookup; then
        echo "Jeg ved ikke noget om $1."
    fi
else
    echo "Men hvad skal jeg forklare?"
fi 
