#!/bin/sh

lookup() {
    cat $CONCIEGGS_DB_DIR/fakta\
 | grep -F -i "$(echo "$EGGS_ARGS")"
}

if [ "$EGGS_ARGS" ]; then
    if ! lookup; then
        echo "Det ved jeg ikke noget om."
    fi
else
    echo "Men hvad skal jeg forklare?"
fi 
