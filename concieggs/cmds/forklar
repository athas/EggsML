#!/bin/sh

lookup() {
     cat $CONCIEGGS_DB_DIR/fakta | grep -F -i "$EGGS_ARGS"
}

if [ "$EGGS_ARGS" ]; then
    if ! lookup; then
        echo "Jeg ved ikke noget om $EGGS_ARGS."
    fi
else
    echo "Men hvad skal jeg forklare?"
fi 
