#!/bin/sh

next=$(nextEggs)
if [ "$next" != "" ]; then
    echo -n "Der er eggs klokken $(timeToDate $next), med "
    cat $CONCIEGGS_DB_DIR/eggs/$next | while read who; do
        echo -n $(randomName "$who")
    done
    echo " tilmeldt."
else
    echo "Ingen har tilmeldt sig eggs!"
fi
