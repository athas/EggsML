#!/bin/sh

next=$(nextEggs)
if [ "$next" != "" ]; then
    echo -n "Der er eggs klokken $(timeToDate $(nowTime)), med "
    cat $CONCIEGGS_DB_DIR/eggs/$next | while read who; do
        echo -n $who
    done
    echo " tilmeldt."
else
    echo "Ingen har tilmeldt sig eggs!"
fi
