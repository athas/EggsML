#!/bin/sh

next=$(nextEggs)
if [ "$next" != "" ]; then
    echo $next | while read eggs; do
        echo "Der er Eggs $(timeToDate $eggs), med "
        cat $CONCIEGGS_DB_DIR/eggs/$eggs | while read who; do
            echo $(randomName "$who")
        done
        echo "tilmeldt."
    done
else
    echo "Ingen har tilmeldt sig eggs!"
fi
